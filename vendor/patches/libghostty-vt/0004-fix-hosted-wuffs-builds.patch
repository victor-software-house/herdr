diff --git a/vendor/libghostty-vt/pkg/wuffs/build.zig b/vendor/libghostty-vt/pkg/wuffs/build.zig
index 2ae4558..3f56e1e 100644
--- a/vendor/libghostty-vt/pkg/wuffs/build.zig
+++ b/vendor/libghostty-vt/pkg/wuffs/build.zig
@@ -38,2 +38,7 @@ pub fn build(b: *std.Build) !void {
     const optimize = b.standardOptimizeOption(.{});
+    const target_libc = b.option(
+        []const u8,
+        "target_libc",
+        "Libc configuration file applied only to target artifacts.",
+    );

@@ -61,2 +66,3 @@ pub fn build(b: *std.Build) !void {
             .link_libc = windows,
+            .libc_file = if (target_libc) |path| .{ .cwd_relative = path } else null,
         });
diff --git a/vendor/libghostty-vt/pkg/wuffs/src/main.zig b/vendor/libghostty-vt/pkg/wuffs/src/main.zig
index ec9f8da..41b74ca 100644
--- a/vendor/libghostty-vt/pkg/wuffs/src/main.zig
+++ b/vendor/libghostty-vt/pkg/wuffs/src/main.zig
@@ -21,8 +21,7 @@ pub const ImageData = struct {
 // and linker garbage collection strips them, but that isn't guaranteed.
-// When libc isn't linked there would be nothing to provide calloc/free if
-// they survive, so export stubs to satisfy the link. Weak so that any real
-// definition wins. Hidden keeps them out of the export table where the
-// format honors it (e.g. wasm).
+// Freestanding consumers may have no allocator to resolve these symbols.
+// Never export stubs for hosted targets: hidden weak symbols in a static
+// archive can override the embedding executable's dynamically linked libc.
 comptime {
-    if (!builtin.link_libc) {
+    if (!builtin.link_libc and builtin.os.tag == .freestanding) {
         @export(&callocStub, .{
diff --git a/vendor/libghostty-vt/src/build/Config.zig b/vendor/libghostty-vt/src/build/Config.zig
index 71fa4d2..5ad530a 100644
--- a/vendor/libghostty-vt/src/build/Config.zig
+++ b/vendor/libghostty-vt/src/build/Config.zig
@@ -74,2 +74,3 @@ is_dep: bool = false,
 env: *const std.process.Environ.Map,
+target_libc: ?[]const u8 = null,

@@ -178,2 +179,7 @@ pub fn init(b: *std.Build, appVersion: []const u8, libVersion: []const u8) !Conf
         .env = env,
+        .target_libc = b.option(
+            []const u8,
+            "target-libc",
+            "Libc configuration file applied only to target artifacts.",
+        ),
     };
diff --git a/vendor/libghostty-vt/src/build/GhosttyLibVt.zig b/vendor/libghostty-vt/src/build/GhosttyLibVt.zig
index 690f864..d1e5ac1 100644
--- a/vendor/libghostty-vt/src/build/GhosttyLibVt.zig
+++ b/vendor/libghostty-vt/src/build/GhosttyLibVt.zig
@@ -232,2 +232,5 @@ fn initLib(
     });
+    if (zig.target_libc) |libc_file| {
+        lib.setLibCFile(.{ .cwd_relative = libc_file });
+    }
     lib.installHeadersDirectory(
diff --git a/vendor/libghostty-vt/src/build/GhosttyZig.zig b/vendor/libghostty-vt/src/build/GhosttyZig.zig
index 46ab05d..98b125e 100644
--- a/vendor/libghostty-vt/src/build/GhosttyZig.zig
+++ b/vendor/libghostty-vt/src/build/GhosttyZig.zig
@@ -22,2 +22,5 @@ simd_libs: SharedDeps.LazyPathList,

+/// Optional libc configuration for target artifacts only.
+target_libc: ?[]const u8,
+
 pub fn init(
@@ -101,2 +104,4 @@ fn initInner(
         .simd_libs = simd_libs,
+
+        .target_libc = cfg.target_libc,
     };
@@ -148,2 +153,3 @@ fn initVt(
             .optimize = cfg.optimize,
+            .target_libc = cfg.target_libc,
         })) |dep| {
